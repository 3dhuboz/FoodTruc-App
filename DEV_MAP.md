# street-eats Dev Map

This file is auto-generated by `C:\Users\Steve\.codex\scripts\codex-dev-map.ps1` to give future Codex sessions a fast project map. Regenerate it with `powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\Steve\.codex\scripts\codex-dev-map.ps1` from the repo root.

## Context Order
- Start here, then read `README.md` and product or plan docs only when they are relevant to the task.
- Prefer the package scripts below for verification before opening large source trees.
- Skip generated and dependency folders such as `node_modules`, `dist`, `build`, `.wrangler`, and coverage outputs unless a task specifically needs them.

## Project Snapshot
- Repo folder: `FoodTruc-App`
- Tracked and unignored source files mapped: 233
- Stack signals: Node.js package, React, Vite, TypeScript, Tailwind CSS, Cloudflare

## Stack Signals
- Node.js package
- React
- Vite
- TypeScript
- Tailwind CSS
- Cloudflare

## Commands
- `npm run build` - `vite build`
- `npm run cap:build` - `vite build && npx cap sync android && cd android && ./gradlew assembleDebug`
- `npm run cap:open` - `npx cap open android`
- `npm run cap:sync` - `vite build && npx cap sync android`
- `npm run db:seed` - `wrangler d1 execute foodtruck-db --file=schema.sql --remote`
- `npm run deploy` - `vite build && wrangler pages deploy dist`
- `npm run dev` - `wrangler pages dev -- vite dev`
- `npm run lint` - `tsc --noEmit`
- `npm run preview` - `wrangler pages dev dist`

## High-Signal Paths
- `functions/api` - 46 mapped files
- `public` - 7 mapped files
- `docs` - 3 mapped files
- `.github/workflows` - 2 mapped files

## Entry Anchors
- `README.md`
- `package.json`
- `vite.config.ts`
- `functions/api/v1/_admin/check-fleet.ts`
- `functions/api/v1/_lib/auth.ts`
- `functions/api/v1/_lib/db.ts`
- `functions/api/v1/_lib/tenant.ts`
- `functions/api/v1/admin/ai-coach.ts`
- `functions/api/v1/admin/analytics.ts`
- `functions/api/v1/admin/fleet.ts`
- `functions/api/v1/admin/fleet/[id].ts`
- `functions/api/v1/admin/orders.ts`
- `functions/api/v1/admin/settings.ts`
- `functions/api/v1/admin/tenants.ts`
- `functions/api/v1/admin/tenants/[id].ts`

## Environment And Automation Anchors
- `.env.example`
- `.github/workflows/build-apk.yml`
- `.github/workflows/ci.yml`
- `tsconfig.json`

## Notes For Future Sessions
- This map is intentionally structural and low-churn: it avoids timestamps, commit hashes, and status output so autosaves do not create empty follow-up commits.
- If a new subsystem becomes important, add or keep its files in normal Git tracking and rerun the generator.

